import view from '../../../src/views/home';

describe('Home View', () => {
  
  it('should exist', () => {
    expect(view).toBeDefined();
  });
  
  it('should render a title', () => {
    expect(1).toBe(1);
  });
});
